import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        width: "100%",
        height: 55,
        justifyContent:"space-around",
        alignItems:"center",
        padding:10
    }
})